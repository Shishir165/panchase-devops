apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "panchase.fullname" . }}-test-connection"
  labels:
    {{- include "panchase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "panchase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

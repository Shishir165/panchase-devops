apiVersion: v1
kind: Service
metadata:
  name: {{ include "panchase.fullname" . }}
  labels:
    {{- include "panchase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "panchase.selectorLabels" . | nindent 4 }}
